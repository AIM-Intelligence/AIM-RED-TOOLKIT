// Export all types from interface.ts for easy importing
export * from './interface';

// You can import types in other files like this:
// import { CreateProjectRequest, ProjectStructure } from '@/types';
// or
// import { CreateProjectRequest, ProjectStructure } from '../types';